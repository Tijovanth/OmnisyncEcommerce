input{
    tcp {
        port => 5000
        host => "0.0.0.0"
        codec => json_lines
    }
}

output{
    elasticsearch{
        hosts => ["elasticsearch:9200"]
        index => "omnisync-ecommerce-logs-%{+YYYY.MM.dd}"
    }
    stdout{
        codec => rubydebug
    }
}